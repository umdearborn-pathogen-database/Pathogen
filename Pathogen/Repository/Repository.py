class my_class(object):
    pass

# saveRawData()
# id, run, mass, intensity, createdDate

# saveFeatureMatrix()
# id(from above), it's feature matrix data, createdDate

# savePCAData()
# id(from above), pcaData, createdDate