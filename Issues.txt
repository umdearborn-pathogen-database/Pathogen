Issues:
	#ONGOING
		Description:
			FeatureMatrix
			PCA
			Database integration
			Cosine ranking?
			Returned pathogen

			Code needs formatting
			Needs redundancy checks
			Needs additional configuration values
			Eventually, files/directories need deleting:
				- Import/*
				- Reference/*
				- TestData/*
				- UML/*
				- *__pycache__*
			Comments need cleaning for:
				- BaselineCorrection.py			-> Jaanvir
				- FeatureMatrix.py
				- Helper.py						-> Jaanvir
				- IntensityCalibration.py		-> Jaanvir
				- PeakBinning.py				-> Jaanvir
				- PeakDetection.py
				- Preprocessing.py
				- QualityControl.py
				- Repository.py
				- Pathogen.py					-> Jaanvir
			Update .gitignore
			Needs README.md
			Needs GitHub cleanup

	#U1. Major - UNRESOLVED
		Description:
			Iteration of inputting data files into dataframe are not in alphabetical order or in the order they
			appear in the directory. For example: 00 dataframe is the BL21-1c csv file, 07 is BL21-1a.
			This causes issues with direct relationship to R script and R studio-run script, fixing this will
			allow for the iteration of the patientID over the existing dataframes without having to add them
			at the beginning.
		Possible fix:
			Iterate through a for loop that pays attention to the file names and also possibly stores the name
			of the bacteria to solve #U2.

# RESOLVED

	#R1. Major - RESOLVED
		Description:
			Any values that are related to dataframes have a type error surrounding it: int vs. str indices

		Fix:
			Data type assumed to be of type dataframe, but were of type list instead, change of type fixed the error.

	#R2. Major - RESOLVED
		Description:
			If BaselineCorrection is applied to every single line import, the load on most computers would not support
			the amount of data being processed at one time.
		
		Fix:
			After reviewing with the client, BaselineCorrection only applies to the first set of data, reducing the load
			and acting in a similar function to the R script.